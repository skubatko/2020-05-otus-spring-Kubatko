spring:
  datasource:
    url: jdbc:h2:mem:library

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-master.xml

  shell:
    interactive:
      enabled: false

app:
  h2ConsoleEnabled: false
